const fs = require('fs');
let file = fs.readFileSync('game/scenes/BattleScene.ts', 'utf8');

file = file.replace(/isPlayer \? -40 : 40/g, 'attacker.x < target.x ? -40 : 40');
file = file.replace(/isPlayer \? 40 : -40/g, 'attacker.x < target.x ? 40 : -40');
file = file.replace(/isPlayer \? -30 : 30/g, 'attacker.x < target.x ? -30 : 30');
file = file.replace(/isPlayer \? 30 : -30/g, 'attacker.x < target.x ? 30 : -30');
file = file.replace(/isPlayer \? 50 : -50/g, 'attacker.x < target.x ? 50 : -50');
file = file.replace(/isPlayer \? -50 : 50/g, 'attacker.x < target.x ? -50 : 50');
file = file.replace(/isPlayer \? 80 : -80/g, 'attacker.x < target.x ? 80 : -80');
file = file.replace(/isPlayer \? -80 : 80/g, 'attacker.x < target.x ? -80 : 80');
file = file.replace(/isPlayer \? 20 : -20/g, 'attacker.x < target.x ? 20 : -20');
file = file.replace(/isPlayer \? -20 : 20/g, 'attacker.x < target.x ? -20 : 20');
file = file.replace(/isPlayer \? 200 : -200/g, 'attacker.x < target.x ? 200 : -200');
file = file.replace(/isPlayer \? -200 : 200/g, 'attacker.x < target.x ? -200 : 200');
file = file.replace(/isPlayer \? 45 : -45/g, 'attacker.x < target.x ? 45 : -45');
file = file.replace(/isPlayer \? -45 : 45/g, 'attacker.x < target.x ? -45 : 45');
file = file.replace(/isPlayer \? 15 : -15/g, 'attacker.x < target.x ? 15 : -15');
file = file.replace(/isPlayer \? -15 : 15/g, 'attacker.x < target.x ? -15 : 15');

file = file.replace(/isP \? -40 : 40/g, 'attacker.x < target.x ? -40 : 40');
file = file.replace(/isP \? 40 : -40/g, 'attacker.x < target.x ? 40 : -40');
file = file.replace(/isP \? 30 : -30/g, 'attacker.x < target.x ? 30 : -30');
file = file.replace(/isP \? -30 : 30/g, 'attacker.x < target.x ? -30 : 30');
file = file.replace(/isP \? 50 : -50/g, 'attacker.x < target.x ? 50 : -50');
file = file.replace(/isP \? -50 : 50/g, 'attacker.x < target.x ? -50 : 50');
file = file.replace(/isP \? 80 : -80/g, 'attacker.x < target.x ? 80 : -80');
file = file.replace(/isP \? -80 : 80/g, 'attacker.x < target.x ? -80 : 80');
file = file.replace(/isP \? 120 : -120/g, 'attacker.x < target.x ? 120 : -120');
file = file.replace(/isP \? -120 : 120/g, 'attacker.x < target.x ? -120 : 120');
file = file.replace(/isP \? 150 : -150/g, 'attacker.x < target.x ? 150 : -150');
file = file.replace(/isP \? -150 : 150/g, 'attacker.x < target.x ? -150 : 150');
file = file.replace(/isP \? 200 : -200/g, 'attacker.x < target.x ? 200 : -200');
file = file.replace(/isP \? -200 : 200/g, 'attacker.x < target.x ? -200 : 200');
file = file.replace(/isP \? 400 : -400/g, 'attacker.x < target.x ? 400 : -400');
file = file.replace(/isP \? -400 : 400/g, 'attacker.x < target.x ? -400 : 400');
file = file.replace(/isP \? 600 : -600/g, 'attacker.x < target.x ? 600 : -600');
file = file.replace(/isP \? -600 : 600/g, 'attacker.x < target.x ? -600 : 600');

fs.writeFileSync('game/scenes/BattleScene.ts', file);
console.log("Fixed more isPlayer positional relative logic");
